<ul>
  <% if Admin::PeopleController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_people_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_person_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::PeopleController.sortable? and Person.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "People"),
                 admin_people_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "People"),
                 admin_people_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>